#ifndef HEADER_H
#define HEADER_H

// Include needed standard library functions (STL)
#include <optional>

// The <signature> declaration(s)
std::optional<char> gimmeChar(char const* char_ptr, int index);

#endif // HEADER_H