#ifndef HEADER_H
#define HEADER_H

// Include needed standard library functions (STL)
#include <string>

// The <signature> declaration(s)
std::string gimmeString(const char char_array[], int length);

#endif // HEADER_H